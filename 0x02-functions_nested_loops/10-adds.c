#include "main.h"
/**
 * add - adds the two values
 * @a: holds the first value
 * @b: holds the second value
 *
 * Return: retuns the sum of the values
 */
int add(int a, int b)
{
	return (a + b);
}
