#include "main.h"
/**
 * _strcat - returns considering case
 * @dest: holds character
 * @src: holds source
 * Return: returns integer
 */
char *_strcat(char *dest, char *src)
{
	return (_strcat(dest, src));
}
