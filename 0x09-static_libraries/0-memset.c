#include "main.h"
/**
 * _memset - returns considering case
 * @s: holds character
 * @b: holds character
 * @n: holds int
 * Return: returns integer
 */
char *_memset(char *s, char b, unsigned int n)
{
	return (_memset(s, b, n));
}
