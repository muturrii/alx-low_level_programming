#include "main.h"
/**
 * _strspn - returns considering case
 * @s: holds character
 * @accept: holds source
 * Return: returns integer
 */
unsigned int _strspn(char *s, char *accept)
{
	return (_strspn(s, accept));
}
