#include "main.h"
/**
 * _strpbrk - returns considering case
 * @s: holds character
 * @accept: holds source
 * Return: returns integer
 */
char *_strpbrk(char *s, char *accept)
{
	return (_strpbrk(s, accept));
}
