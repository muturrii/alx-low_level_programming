#include "main.h"
/**
 * mul - multiplies the two values
 * @a: holds the first value
 * @b: holds the second value
 *
 * Return: retuns the multiplication of the values
 */
int mul(int a, int b)
{
	return (a * b);
}
