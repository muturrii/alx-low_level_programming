#include <stdio.h>

/**
 * main - prints the string
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char wah[] = "\"Programming is like building a multilingual puzzle";

	puts(wah);
	return (0);
}
